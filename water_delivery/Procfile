# =============================================
# PROCFILE PARA DESPLIEGUE EN RAILWAY/HEROKU
# =============================================
# Define cómo se ejecuta la aplicación en producción

web: sh -c "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn water_delivery.wsgi:application --bind 0.0.0.0:$PORT"